<nav class="navbar navbar-expand-sm navbar-light bg-light mb-4">
    <a class="navbar-brand" href="#">Symfony 6</a>
    <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#collapsibleNavId" aria-controls="collapsibleNavId"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="collapsibleNavId">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home_index') }}">Home</a>
            </li>
            {% if is_granted("ROLE_CREATOR") %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home_create') }}">Add</a>
            </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
            <li class="nav-item">
                <a href="#" class="nav-link">{{ app.user.email }}</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a href="{{ path('app_login') }}" class="nav-link">Login</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
<!--% if message %}
    <h3>{ message }}</h3>
%endif %}--> 